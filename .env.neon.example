# Neon Database Configuration (Recommended Free Alternative)
# Get these values from neon.tech

# Database Connection
DATABASE_URL="postgresql://[user]:[password]@[neon-hostname].neon.tech/[dbname]?sslmode=require&connect_timeout=10"

# Alternative: If using Vercel Postgres (also free)
# DATABASE_URL="postgres://default:[password]@[host]/[dbname]?sslmode=require"

# Database Pooling (for production)
# DATABASE_URL="postgresql://[user]:[password]@[neon-hostname]-pooler.neon.tech/[dbname]?sslmode=require"

# Connection Settings
DATABASE_POOL_SIZE=10
DATABASE_TIMEOUT=10000